name: Debug Wii

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkitppc:latest

    steps:
      - name: List files
        run: |
          echo "Derleyiciyi ariyorum..."
          find / -name "powerpc-eabi-g++" 2>/dev/null || echo "Bulunamadi!"
          echo "Dizinleri listeliyorum..."
          ls -R /opt/devkitpro
